/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		remotePatterns: [
			{
				hostname:
					"hljcmoxhoxlzloofamyr.supabase.co",
				protocol: "https",
				port: "",
			},
		],
	},
}

export default nextConfig
